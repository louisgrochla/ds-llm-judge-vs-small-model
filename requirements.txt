# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11

# Deep learning + transformers
torch>=2.1
transformers>=4.40
datasets>=2.16,<4.0  # pinned: v4 removed script-based dataset loading, breaks PolyAI/banking77
evaluate>=0.4
accelerate>=0.27

# LLM API
# >=0.104 for output_config (structured outputs with a closed label enum) and the
# typed Batch API helpers the eval harness depends on. Verified against 0.104.1.
anthropic>=0.104

# HF Hub (for downloading Banking77 parquet directly — bypasses the broken script loader)
huggingface_hub>=0.20

# Notebook + viz
jupyter>=1.0
ipykernel>=6.27
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# Utilities
tqdm>=4.66
python-dotenv>=1.0
